#include <stdio.h>

int main(){
    int i,n;
    char ch[100];
    scanf("%s",&ch);
    for(i=0;ch[i]!='\0';i++)
    
    
    {
        if(ch[i]>=97 && ch[i]<=122)
        {
            printf("%c",ch[i]-32);
        }
        if(ch[i]>=65 && ch[i]<=90)
             printf("%c",ch[i]+32);
    }
}
